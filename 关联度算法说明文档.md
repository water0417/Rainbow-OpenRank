# GitHub仓库三维关联度算法说明文档

## 概述

本文档详细说明了用于计算GitHub仓库之间关联度的三维算法。该算法综合考虑了生态关联、语义关联和时序关联三个维度，通过加权融合得到最终的关联度分数。

## 算法架构

```
总体关联度 = 0.4 × 生态关联度 + 0.4 × 语义关联度 + 0.2 × 时序关联度
```

## 一、生态关联度 (Ecological Similarity) - 权重 40%

### 1.1 贡献者重叠度 (40%)

使用Jaccard相似系数计算两个仓库的贡献者重叠程度。

**公式：**

```
贡献者相似度 = |贡献者_i ∩ 贡献者_j| / |贡献者_i ∪ 贡献者_j|
```

**实现：**

```python
contributors_i = set([c['login'] for c in repo_i['contributor_network']])
contributors_j = set([c['login'] for c in repo_j['contributor_network']])
intersection = len(contributors_i & contributors_j)
union = len(contributors_i | contributors_j)
contributor_sim = intersection / union if union > 0 else 0.0
```

**特点：**

- 值域：[0, 1]
- 1表示完全相同的贡献者集合
- 0表示无共同贡献者

### 1.2 语言相似度 (30%)

判断两个仓库是否使用相同的编程语言。

**公式：**

```
语言相似度 = 1.0  如果 language_i == language_j 且 language != ''
语言相似度 = 0.0  其他情况
```

**特点：**

- 二元分类：相同语言=1，不同语言=0
- 突出技术栈相似性

### 1.3 主题重叠度 (20%)

使用Jaccard相似系数计算两个仓库的GitHub Topics重叠程度。

**公式：**

```
主题相似度 = |topics_i ∩ topics_j| / |topics_i ∪ topics_j|
```

**实现：**

```python
topics_i = set(repo_i['topics']) if repo_i['topics'] else set()
topics_j = set(repo_j['topics']) if repo_j['topics'] else set()
intersection = len(topics_i & topics_j)
union = len(topics_i | topics_j)
topic_sim = intersection / union if union > 0 else 0.0
```

**特点：**

- 值域：[0, 1]
- 反映仓库的功能和用途相似性

### 1.4 Star数量相似度 (10%)

计算两个仓库Star数量的相对接近程度。

**公式：**

```
Star相似度 = min(star_i, star_j) / max(star_i, star_j)
```

**特点：**

- 值域：[0, 1]
- 1表示Star数量相同
- 反映项目规模和流行度的相似性

### 1.5 综合生态关联度

**公式：**

```
生态关联度 = 0.4 × 贡献者相似度 + 
             0.3 × 语言相似度 + 
             0.2 × 主题相似度 + 
             0.1 × Star相似度
```

## 二、语义关联度 (Semantic Similarity) - 权重 40%

### 2.1 文本特征提取

从以下来源提取文本特征：

- **描述** (description)
- **主题** (topics)
- **编程语言** (language)
- **README内容** (readme_content，前3000字符)

**文本预处理：**

```python
def clean_text(text):
    # 移除HTML标签
    text = re.sub(r'<[^>]+>', '', text)
    # 移除Markdown链接
    text = re.sub(r'\[.*?\]\(.*?\)', '', text)
    # 移除特殊字符
    text = re.sub(r'[^\w\s]', ' ', text)
    # 合并空格
    text = re.sub(r'\s+', ' ', text)
    return text.strip().lower()
```

### 2.2 TF-IDF向量化

使用增强的TF-IDF参数：

```python
vectorizer = TfidfVectorizer(
    max_features=8000,      # 特征数量
    ngram_range=(1, 3),     # 1-3元组
    min_df=1,               # 最小文档频率
    max_df=0.95,            # 最大文档频率
    stop_words='english',   # 停用词
    sublinear_tf=True       # 次线性TF缩放
)
```

**参数说明：**

- `max_features=8000`：保留最重要的8000个特征
- `ngram_range=(1, 3)`：捕获单词、双词组和三词组
- `sublinear_tf=True`：使用1+log(tf)代替tf，减少长文本影响
- `max_df=0.95`：过滤出现在95%以上文档中的词

### 2.3 余弦相似度

**公式：**

```
语义相似度 = cosine_similarity(TF-IDF_i, TF-IDF_j)
```

### 2.4 语言相似度融合

将语言相似度作为额外语义特征融入：

**公式：**

```
融合语义相似度 = 0.8 × TF-IDF相似度 + 0.2 × 语言相似度
```

### 2.5 综合语义关联度

直接使用融合后的语义相似度作为语义关联度。

## 三、时序关联度 (Temporal Similarity) - 权重 20%

### 3.1 Star增长率计算

从绝对Star数转换为增长率，更好反映趋势：

**公式：**

```
增长率_t = (Star_t - Star_{t-1}) / Star_{t-1}  如果 Star_{t-1} > 0
增长率_t = 0  如果 Star_{t-1} == 0
```

**实现：**

```python
def extract_stars_growth_rate(stars_timeline):
    sorted_timeline = sorted(stars_timeline, key=lambda x: x['month'])
    stars_values = [item['stars'] for item in sorted_timeline]
  
    growth_rates = []
    for i in range(1, len(stars_values)):
        if stars_values[i-1] > 0:
            growth_rate = (stars_values[i] - stars_values[i-1]) / stars_values[i-1]
        else:
            growth_rate = 0
        growth_rates.append(growth_rate)
  
    return np.array(growth_rates[:23])
```

### 3.2 动态时间规整 (DTW)

DTW算法用于比较两个时间序列的相似性，允许时间轴上的弹性对齐。

**算法步骤：**

1. **构建DTW矩阵：**

```python
def calculate_dtw_distance(series_a, series_b):
    n, m = len(series_a), len(series_b)
    dtw_matrix = np.full((n + 1, m + 1), np.inf)
    dtw_matrix[0, 0] = 0
  
    for i in range(1, n + 1):
        for j in range(1, m + 1):
            cost = abs(series_a[i - 1] - series_b[j - 1])
            dtw_matrix[i, j] = cost + min(
                dtw_matrix[i - 1, j],      # 插入
                dtw_matrix[i, j - 1],      # 删除
                dtw_matrix[i - 1, j - 1]   # 匹配
            )
  
    return dtw_matrix[n, m]
```

2. **全局归一化：**

```python
# 计算所有仓库对的最大DTW距离
max_global_dist = 0
for i in range(n):
    for j in range(i + 1, n):
        dist = calculate_dtw_distance(growth_rates[i], growth_rates[j])
        if dist > max_global_dist:
            max_global_dist = dist
```

### 3.3 时序相似度计算

**公式：**

```
时序相似度 = max(0, 1 - DTW距离 / 全局最大DTW距离)
```

**特点：**

- 值域：[0, 1]
- 1表示完全相同的增长趋势
- 0表示完全不同的增长趋势
- 使用全局最大距离确保归一化的一致性

## 四、总体关联度计算

### 4.1 加权融合

**公式：**

```
总体关联度 = 0.4 × 生态关联度 + 
             0.4 × 语义关联度 + 
             0.2 × 时序关联度
```

### 4.2 对角线处理

确保每个仓库与自身的关联度为1.0：

```python
for i in range(n):
    total_matrix[i, i] = 1.0
```

## 五、算法优化要点

### 5.1 生态关联度优化

1. **多维度融合**：不仅考虑贡献者，还加入语言、主题和Star数量
2. **权重分配**：贡献者40%，语言30%，主题20%，Star10%
3. **Jaccard系数**：用于贡献者和主题重叠计算，效果优于简单计数

### 5.2 语义关联度优化

1. **增强TF-IDF**：

   - n-grams从(1, 2)扩展到(1, 3)
   - 特征数从5000增加到8000
   - 启用sublinear_tf=True
2. **语言融合**：将语言相似度作为额外特征，提升20%权重
3. **文本扩展**：README截取长度从2000增加到3000字符

### 5.3 时序关联度优化

1. **增长率转换**：从绝对Star数改为增长率，更好反映趋势
2. **全局归一化**：使用全局最大DTW距离，避免局部归一化问题
3. **非负保证**：使用max(0, ...)确保相似度非负

### 5.4 权重调整

从原来的(0.5, 0.3, 0.2)调整为(0.4, 0.4, 0.2)，提升语义关联度权重。

## 六、算法性能指标

### 6.1 数据覆盖

- 总仓库数：722
- 贡献者网络：721/722 (99.9%)
- Star时序数据：718/722 (99.4%)
- 描述信息：716/722 (99.2%)
- 主题信息：610/722 (84.5%)
- README内容：720/722 (99.7%)

### 6.2 关联度分布

| 维度       | 均值   | 标准差 | 最小值 | 最大值 |
| ---------- | ------ | ------ | ------ | ------ |
| 生态关联度 | 0.0709 | 0.0946 | 0.0000 | 1.0000 |
| 语义关联度 | 0.0499 | 0.0697 | 0.0000 | 1.0000 |
| 时序关联度 | 0.9941 | 0.0413 | 0.0000 | 1.0000 |
| 总体关联度 | 0.2471 | 0.0644 | 0.0456 | 1.0000 |

### 6.3 优化效果

| 指标             | 优化前 | 优化后 | 改进   |
| ---------------- | ------ | ------ | ------ |
| 生态关联度均值   | 0.0052 | 0.0709 | +1263% |
| 语义关联度均值   | 0.0325 | 0.0499 | +54%   |
| 总体关联度标准差 | 0.0316 | 0.0644 | +104%  |
| 最小总体关联度   | 0.2000 | 0.0456 | -77%   |

## 七、应用场景

1. **仓库推荐**：根据关联度推荐相似仓库
2. **技术栈分析**：识别相关技术栈和工具链
3. **社区发现**：发现紧密关联的仓库集群
4. **趋势预测**：通过时序关联预测技术趋势
5. **社交网络可视化**：构建仓库关联网络图

## 八、使用示例

### 8.1 计算两个仓库的关联度

```python
import json
import numpy as np

# 加载结果
with open('repo_similarity_result_722_optimized.json', 'r') as f:
    result = json.load(f)

# 查找特定仓库对的关联度
repo1 = "flutter/flutter"
repo2 = "flutter/engine"

# 在top_similar_pairs中查找
for pair in result['top_similar_pairs']:
    if (pair['repo1'] == repo1 and pair['repo2'] == repo2) or \
       (pair['repo1'] == repo2 and pair['repo2'] == repo1):
        print(f"总体关联度: {pair['total_score']:.4f}")
        print(f"生态关联度: {pair['eco_score']:.4f}")
        print(f"语义关联度: {pair['semantic_score']:.4f}")
        print(f"时序关联度: {pair['temporal_score']:.4f}")
        break
```

### 8.2 构建仓库关联网络

```python
import pandas as pd
import networkx as nx
import matplotlib.pyplot as plt

# 加载相似度矩阵
similarity_df = pd.read_csv('repo_similarity_matrix_722_optimized.csv', index_col=0)

# 设置阈值，只保留强关联
threshold = 0.5
G = nx.Graph()

# 添加节点和边
for i, repo1 in enumerate(similarity_df.index):
    for j, repo2 in enumerate(similarity_df.columns):
        if i < j and similarity_df.iloc[i, j] >= threshold:
            G.add_edge(repo1, repo2, weight=similarity_df.iloc[i, j])

# 可视化
plt.figure(figsize=(20, 20))
pos = nx.spring_layout(G, k=0.3, iterations=50)
nx.draw(G, pos, 
        node_size=100,
        node_color='lightblue',
        edge_color='gray',
        with_labels=True,
        font_size=8)
plt.title('GitHub Repository Similarity Network (threshold >= 0.5)')
plt.savefig('repo_similarity_network.png', dpi=300, bbox_inches='tight')
plt.show()
```

## 九、注意事项

1. **数据质量**：关联度计算依赖于数据完整性，缺失数据会影响结果准确性
2. **时间窗口**：时序关联度基于24个月的Star数据，可能不反映长期趋势
3. **语言限制**：TF-IDF使用英文停用词，对非英文内容效果可能不佳
4. **计算复杂度**：722个仓库的相似度矩阵计算需要约5-10分钟
5. **阈值选择**：网络可视化需要根据应用场景选择合适的关联度阈值
